from setuptools import setup, find_packages
 
setup(
    name="PMOO",
    # version="0.1",
    packages=find_packages(),
    install_requires=[
        # list of your project's dependencies
    ],
    python_requires='>=3.9',
)